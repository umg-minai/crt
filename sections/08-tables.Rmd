# Tables

```{r table1, echo = FALSE, warning = FALSE, message = FALSE, cache = TRUE}
theme_gtsummary_journal()
theme_gtsummary_compact(font_size = 8L)
tbl1 <- cvc |>
dplyr::filter(!isExcluded) |>
select(
    CRT,
    Sex,
    Age,
    BMI,
    Manufacturer,
    Lumens,
    Site,
    Side,
    Incision,
    Attempts2,
    Experience,
    AdmissionType,
    Speciality,
    Sepsis,
    Cancer,
    DVT,
    AnticoagulationGroup,
    FirstExam,
    WBC1,
    CRP1,
    DDimer1
) |>
tbl_summary(
    by = CRT,
    missing_text = "(Missing)",
    type = list(
        Sex ~ "dichotomous",
        DVT ~ "dichotomous",
        Side ~ "dichotomous",
        Attempts2 ~ "dichotomous"
    ),
    label = list(
        Sex ~ "Sex (male)",
        DVT ~ "History of venous thrombosis",
        Side ~ "Side of insertion (left)",
        Site ~ "Site of insertion",
        Attempts2 ~ "More than one insertion attempt",
        Lumens ~ "Number of catheter lumens",
        AnticoagulationGroup ~ "Type of anticoagulation drug",
        FirstExam ~ "Time to first exam [hours]",
        WBC1 ~ "WBC day 1 [Gpt/L]",
        DDimer1 ~ "D-dimer day 1 [mg/L]",
        CRP1 ~ "CRP day 1 [mg/L]"
    ),
    value = list(
        Sex ~ "male",
        DVT ~ "yes",
        Side ~ "left",
        Attempts2 ~ ">1",
        Incision ~ "yes"
    )
) |>
add_overall() |>
add_p(test.args = c(Speciality) ~ list(simulate.p.value = TRUE))

uvar <- unique(sort(c(tbl1$table_body$variable, tbl1$table_body$label)))
.abbr <- Filter(nchar, abbr[uvar])

tbl1 |>
modify_caption(
    paste0(
        "Baseline characteristics.<br />",
        "Values are given as ",
        "median (lower quartile - upper quartile) or n (percent).<br />",
        paste(names(.abbr), .abbr, sep = ", ", collapse = "; ")
    )
)
```

```{r tblml3, echo = FALSE}
ml3$sft
ml3$svd
```

```{r tblml5, echo = FALSE}
ml5$sft
ml5$svd
```

```{r tblmlA, echo = FALSE}
mlA$sft
mlA$svd
```
